# Copy this file to `.env` (same directory) and paste your real key in.
# `.env` is gitignored, so the key never reaches the repository.
#
#   cp .env.example .env
#
# Get a key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides:
# CLAUDE_MODEL=claude-sonnet-5
